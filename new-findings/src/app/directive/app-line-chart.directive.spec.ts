import { AppLineChartDirective } from './app-line-chart.directive';

describe('AppLineChartDirective', () => {
  it('should create an instance', () => {
    const directive = new AppLineChartDirective();
    expect(directive).toBeTruthy();
  });
});
