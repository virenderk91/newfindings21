import { GrpupBarChartDirective } from './grpup-bar-chart.directive';

describe('GrpupBarChartDirective', () => {
  it('should create an instance', () => {
    const directive = new GrpupBarChartDirective();
    expect(directive).toBeTruthy();
  });
});
